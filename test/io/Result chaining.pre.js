score = await db.get(input) >> JSON.parse(*) >> parseInt(*.value) / 100 //use of asterix in identifier position in expression after >> operator
bitshift = 9 >> 3 // no use of asterix
bitshift = 152 >> 2 * 3 //use of asterix in operator position, not in identifier position.